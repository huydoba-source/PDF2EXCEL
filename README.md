# PDF Extractor

### Local Setup
1. **Clone the repo:** `git clone <repo-url>`
2. **Install deps:** `pip install -r requirements.txt`
3. **Run:** `streamlit run src/app.py`

### Docker Setup
1. **Build:** `docker build -t decathlon-pdf .`
2. **Run:** `docker run -p 8501:8501 decathlon-pdf`
